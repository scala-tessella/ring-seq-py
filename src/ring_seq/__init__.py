"""Extends `list`, `tuple` and `str` with ring (circular) methods.

Add new operations to `list`, `tuple` and `str`
for when such a sequence needs to be considered **circular**,
its elements forming a ring.

Modules exported by this package:

- `methods`: Contains all the library methods plus new types.
- `RingSeq`: Contains the `RingSeq` class.
"""
from ring_seq.RingSeq import RingSeq
